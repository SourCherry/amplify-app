import { OggdudeUploadDirective } from './oggdude-upload.directive';

describe('OggdudeUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new OggdudeUploadDirective();
    expect(directive).toBeTruthy();
  });
});
